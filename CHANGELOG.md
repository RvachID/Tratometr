# Changelog — Тратометр
## В планах
- Статистика (неделя/месяц), экспорт/импорт.
  Приняла. Ты прав: **новая сущность → 0.9.0**, и формат держим **строго как в образце**. Кратко, по делу, без лирики.

---

## [0.9.0] - 2026-01-14

### Добавлено

* Новая сущность **«Список покупок» (AliceItem)** с отдельной страницей управления.
* Поддержка закреплённых товаров (регулярные покупки) и архива купленных.
* Мобильное управление списком через **свайпы**:

  * вправо — закрепить / открепить;
  * влево — удалить с возможностью отмены (Undo).
* Группировка товаров в списке и в выпадающем селекте сканера:

  * «Регулярные покупки» (📌),
  * «Остальное»,
  * «Архив».

### Изменено

* Выпадающий список товаров при сканировании теперь синхронизируется с основным списком без перезагрузки страницы.
* Редактирование названия товара на странице списка выполняется **inline** с сохранением по потере фокуса.
* Логика работы со списком вынесена в сервисный слой (`AliceListService`).

### Исправлено

* Рассинхронизация статуса `is_done` товаров между разными покупочными сессиями.
* Ошибки сохранения названия товара при асинхронном обновлении.
* Конфликты CSS, приводившие к «приплюснутым» чекбоксам и некорректной вёрстке на десктопе и мобильных экранах.

---

## [0.8.1] - 2025-12-02
### Добавлено
- Обработка «мелких копеек» по Overlay: если основная цена распознана как целое число, а рядом справа/чуть выше есть отдельная группа из двух цифр меньшим шрифтом (149 + 99), такая пара интерпретируется как **149.99**. Работает только для кейсов вида `149⁹⁹`, не затрагивая форматы `3.999`, `1.199` и обычные `29.99`.

### Изменено
- `ScanService::runRecognition()` теперь всегда использует единый пайплайн `parseImage() → extractAmountByOverlay() → extractAmount()`. Экспериментальный вызов `OcrClient::extractPriceFromImage()` полностью убран.

### Исправлено
- Фатальные ошибки `Call to undefined method app\components\OcrClient::extractPriceFromImage()` при сканировании, которые приводили к алертам «ошибка OCR-запроса» и подвисанию приложения до истечения таймаута внешнего OCR-сервиса.

## [0.8.0] - 2025-10-29
### Добавлено
- Выделен бизнес-слой (`app/services/*`), контроллеры работают через сервисы вместо встроенных монолитных методов.

### Изменено
- Упрощены `ScanController`, `PriceController`, `SiteController`, `AuthController` — осталась только HTTP-обвязка и вызовы сервисов.
- Страница истории теперь корректно отображает русские подписи и реальные даты сессий; удаление сессии работает стабильно.

## [0.7.4] - 2025-10-03
### Исправлено
- **Автоматически закрытые сессии не попадали в историю и статистику.**  
  При достижении TTL активная сессия закрывалась «тихо» без пересчёта кешей, из-за чего:
  - в `history` не было записи,
  - в `stats` суммы игнорировали такую сессию.

  Теперь при ленивом обращении к активной сессии выполняется корректная финализация:
  `PurchaseSessionService::active()` вызывает `finalize()` (под транзакцией) с заполнением
  `total_amount`, `limit_left`, `closed_at` и переводом статуса в *closed*.  
  История и статистика начинают видеть такую сессию сразу после автозакрытия.

### Примечания к изменению
- Ручное закрытие не затронуто и работает как раньше.
- Для продакшена TTL берётся из `components['ps']['autocloseSeconds']`.

  [0.7.3] — 2025-08-29
### Added
- Распознавание копеек мелким шрифтом:
  - новая функция `refinePriceFromCrop()` — делает кроп вокруг основной цены и повторный OCR;
  - новая функция `tryFindCentsViaRoi()` — читает копейки в ROI справа-сверху от числа.
- Фильтрация зачёркнутых ценников: зачёркнутые токены в Overlay теперь исключаются из парсинга.

### Changed
- Кнопка «История»: заменён значок 📜 на 🗒️ для единообразия со стилем интерфейса.
- Унификация отображения `total`: теперь всегда строка с двумя знаками после запятой.
- В `actionUpdate` добавлен фильтр по `session_id` (как в `actionDelete`) для защиты от чужих сессий.
- В `actionRecognize`:
  - проверка MIME-типа загружаемого файла;
  - функция `enforceSizeLimit()` для автоматического сжатия изображения до лимита 1 МБ;
  - удаление временных файлов вынесено в общий `finally`.

### Fixed
- Исправлено деление на 100 для больших сумм без копеек (например, `12999` → `12.99`). Теперь деление на 100 применяется только если группа цифр ≤3 или при явных копейках.
[0.7.2] — 2025-08-28

### Added

Honeypot-поле на формах login/signup (hp) для отсечения ботов.

Анти-авто-submit таймер (render_ts): отклоняем отправки быстрее 2–3 сек от рендера.

Rate-limit логина: не более 5 попыток за 15 минут на пару (IP+email).

Мягкий backoff на ошибках логина (задержка ответа 100–500 мс, растущая).

Базовая отсечка одноразовых e-mail доменов при регистрации (минимальный список + эвристики по домену).

### Changed

AuthController: добавлены предвалидации (honeypot/таймер/лимиты) до проверки пароля; сообщения и логи приведены к единому стилю.

Логирование подозрительных попыток (honeypot/лимит) через Yii::warning/Yii::info.

### Notes

Для работы лимитов/бэкоффа требуется настроенный Yii::$app->cache (в конфиге есть FileCache — подходит).

[0.7.1] — 2025-08-28

### Added

Приветственный блок на экране входа: краткое описание проекта и ссылка «Подробнее» на /site/about.

Поддержка часовых поясов пользователей (IANA): автодетект в браузере, передача в формы, сохранение в user.timezone, показ дат/времени в TZ пользователя через поведение 'as timezone'.

### Changed

Центровка UI: кнопки на экране сканирования и копирайт/верхнее меню на широких экранах.

Guest-layout приведён к каноническому виду: единые CSRF-метатеги, корректный порядок beginPage/head/beginBody.

### Fixed

Случайные 400 при логине/регистрации из-за CSRF и дублей метатегов; добавлен резерв передачи tz через POST.

Корректное применение часового пояса на всех страницах (поведение вынесено в корень конфига как 'as timezone').
# Changelog
[0.7.0] — 2025-08-21
### Added

Кэш по сессиям в БД (в копейках): total_amount, limit_left, closed_at. Подсчёт в finalize().

Статистика: страница /site/stats + JSON /site/stats-data.

Фильтры: период (date-from/to), категории (чекбоксы).

Диаграмма: круговая (pie) с подписями на секторах (проценты/названия), брендовая «бронзовая» палитра.

Заглушка «Нет данных…», постоянная подпись под графиком: «Учитываются только завершённые сессии».

Кнопка «О проекте» на главной → /site/about.

### Changed

История: для закрытых сессий используем кэш (total_amount/limit_left); для активной — считаем «вживую». price_entry джоинится только для активной.

Flash-алерты: автозакрытие ≈3 с; success — в бронзовой пастели, danger — мягкий пастельный красный.

Мобильное меню (бургер): закрывается тапом вне меню; клики внутри меню не закрывают его; поддержка закрытия по Esc.

Статистика (клиент): относительные URL, корректная работа с параметром r=, безопасный парсинг JSON, обновление адресной строки через URL/searchParams.

### Fixed

Неверный расчёт «остатка по лимиту» из‑за смешения рублей/копеек → теперь все расчёты в копейках (INT), вывод — в рублях.

Во флеше «закрытие сессии» остаток показывается только если лимит был задан.

Редкие 404/Mixed Content на странице статистики (HTTP→HTTPS, потеря r, пустые наборы данных).

### Migration / Schema

Таблица purchase_session (создана/пересоздана):

Поля: id, user_id, shop, category, limit_amount INT NULL (копейки), status TINYINT, started_at INT, updated_at INT, total_amount INT NOT NULL DEFAULT 0, limit_left INT NULL, closed_at INT NULL.

Индексы: idx_ps_user_status(user_id,status), idx_ps_user_closed(user_id,closed_at), idx_ps_user_category(user_id,category), idx_ps_status(status).

price_entry: session_id INT NULL, индекс idx_price_entry_session(session_id).
## v0.6.0 — 2025-08-20
### Добавлено
- История покупок (/site/history): список сессий с последним временем, магазином, категорией и суммой:
  - «Итого» (без лимита),
  - «До лимита» (с лимитом; уходит в минус, красным и жирным при превышении).
- Адаптивное отображение на мобильных без горизонтального скролла, кнопка «← На главную».

### Изменено
- Главная: скрываем «🛒 За покупками», если есть открытая сессия (остаются «Продолжить / Закончить / Удалить»).
- Сканер: после «Сохранить» камера автоматически закрывается, чтобы не мешала просмотру списка.

### Исправлено
- Отображение лимита в рублях на главной (ранее показывались копейки).

## [0.5.0] - 2025-08-20
### Added
- Опциональный **лимит траты** при старте сессии (поле в модалке «Магазин/Категория»).
- Отображение «**До лимита:**» вместо «Общая сумма» на странице скана, вычисление как `лимит − сумма_сессии`, уход в минус — красным и жирным.
- Вывод лимита на главной (в рублях, с форматированием).
- Кнопка **«Отмена»** OCR рядом с «Сканировать» + хард-таймаут; корректный сброс UI.
- Выделение суммы **по клику** в модалке (select-all), без автопоказа клавиатуры при открытии.
- Показ отправленного снимка **по кнопке «Показать скан»** в модалке (по умолчанию скрыт).

### Changed
- Размер кадра для OCR масштабируется до `MAX_W=1600` (качество/скорость), клиентская Ч/Б-обработка отключена по умолчанию.
- Уточнён протокол: `session-status` теперь возвращает `limit`, `begin-ajax` принимает `limit` (в рублях строкой; на бэке приводим к копейкам).

### Fixed
- Исправлено: подпись тотала и расчёты корректно переключаются на режим «До лимита».
- Исправлено: ошибка `FormData` при старте сессии (инициализация переменной до `append`).
- Мелкие правки UI и устойчивости.

### Dev notes
- Если столбца `purchase_session.limit_amount` не было — добавь миграцию (int, копейки). У тебя он уже есть.

## v0.4.2 — 2025-08-20
### Added
- Кнопка **«Отмена»** для OCR на странице «Скан» (AbortController + жёсткий таймаут 12с).  
  При отмене/таймауте UI корректно возвращается в исходное состояние.
- Выделение **суммы по клику** (select-on-focus) без автопоказа клавиатуры при открытии модалки.

### Changed
- Показ скана: по умолчанию **скрыт**; открывается кнопкой «Показать скан».  
  На странице превью выводится **только при ошибке OCR**. В модалке показывается именно тот кадр,
  который отправлялся на сервер (через `lastPhotoURL`).
- Лёгкое **масштабирование** кадра перед отправкой (MAX_W=1600) для уменьшения веса запроса.

### Technical
- Добавлен флаг `USE_CLIENT_BW=false` (опциональная клиентская Ч/Б-обработка).
- Исправлены возможные ошибки `ReferenceError` (ранний `putImageData`, неинициализированный `MAX_W`).

## [0.4.1] - 2025-08-19
### Added
- Кнопка **«Отмена»** в модалке скана; принудительная отмена OCR (AbortController) и жёсткий таймаут (12с) с корректным сбросом спиннера.
- Повторный OCR из модалки без её закрытия (по последнему снимку).
- Автофокус и авто-выделение поля суммы при открытии модалки; крупный центрированный инпут цены.

### Changed
- Карточки позиций: вывод **«Итого по позиции»** (цена × количество) с пересчётом на лету; кнопка «Удалить» перенесена вправо в той же строке.
- Поле «Цена» внутри карточки — выравнивание по центру.

### Fixed
- «Залипание» кнопки сканирования при сбоях OCR: теперь запрос можно отменить и сразу повторить.

### Migrations
- Нет.

## [0.4.0] - 2025-08-17
### Added
- Серверные сессии покупок: таблица `purchase_session`, связь `price_entry.session_id`.
- Восстановление активной сессии при перезаходе; панель на главной с кнопками **Продолжить / Закончить / Удалить**.
- Компонент `PurchaseSessionService` (`Yii::$app->ps`) с авто-закрытием сессии после 3 часов неактивности.

### Changed
- Экран сканирования: больше не зависит от PHP-сессии; выборка и итог строятся по `session_id`.
- Все create/update/delete записей жёстко привязаны к активной серверной сессии.

### Removed
- Клиентская `shopSession`, таймауты `ASK/RESET` и модалка «продолжить?».

### Security / Auth
- Переход с PIN на хешированный пароль (`password_hash`), обновлены формы регистрации/входа/смены пароля.

### Fixed
- Пропадание позиций после F5 на скане.
- Необновление итога «на лету» (исправлен селектор и JSON-ответы `scan/update`, `scan/delete`).
- Несогласованность пересчёта при изменении `qty/amount`.

### Migrations
- `m250815_000001_replace_pin_with_password_hash` — добавлен `password_hash`, удалён `pin_code`.
- `m250817_000001_purchase_session_and_link` — добавлена таблица `purchase_session`, колонка `price_entry.session_id`.
> Примечание: разово привяжи осиротевшие `price_entry` к активным сессиям SQL-скриптом (см. тред).
> 
## [0.3.0] — UX/стиль, цитаты, версия (текущая)
### Added
- Отдельная таблица `quotes` + вывод случайной цитаты на главной (малый кегль), защита от повторов подряд через сессию.
- Футер с ручной версией приложения из `params.php` (`'version' => '0.3.0'`).

### Changed
- Тёплая «бронзово-металлическая» шапка: градиент `#7C4F35 → #C19A6B` (перебивает `.navbar-dark.bg-dark`).
- Единая цветовая тема: золотистые акценты для `btn-outline-secondary`, заголовков и цитаты.
- Цвет фокуса инпутов/селектов под тему (бордер и glow в золотистой гамме).
- Заголовки и тексты приведены к минималистичному стилю.

### Fixed
- Правильная видимость управляющих элементов (кнопки «Скан», «Переснять») в разных режимах.

---

## [0.2.1] — Поведение модалок и камеры
### Added
- Автовозврат кнопок «Скан» и «Переснять» при открытии камеры.
- Скрытие «Скан» и «Переснять» в модалке ручного ввода.

### Changed
- Кнопка «Открыть/Закрыть камеру» корректно меняет текст; при ошибке открытия камера скрывается, `manualBtn` снова доступна.

--

## [0.2.0] — Сессии, заголовки, фильтры
### Added
- Сессия магазина/категории на скане; редирект на старт при отсутствии сессионных данных.
- Таймауты: `ASK_THRESHOLD_SEC` (модалка «продолжить?») и `RESET_THRESHOLD_SEC` (сброс сессии с редиректом).
- Заголовок «Покупаем: {категория}. В магазине: {магазин}» (уменьшенный кегль, обновляется по `hidden.bs.modal`).
- JS читает `data-store` / `data-category` из `#scan-root` + `updateScanTitle()`.

### Fixed
- Фильтрация: на экране сканирования отображаются и суммируются только записи **текущей сессии**.
- Исправлен пересчёт итога при изменении `qty`/удалении — больше не затрагиваются записи вне сессии.

---

## [0.1.2] — Точечные фиксы
### Fixed
- Стабильность работы сканера: корректная инициализация/остановка стрима, сброс предпросмотра фото.
- Мелкие правки верстки (камера, отступы), чтобы элементы не срезались.

---

## [0.1.1] — Ручной ввод
### Added
- Модалка ручного ввода: предзаполнение значений, сброс заметки и предпросмотра фото, единый форматтер сумм.

---

## [0.1.0] — MVP
### Added
- Стартовая («Тратометр») с переходом на `site/scan`.
- Экран сканирования: открытие/закрытие камеры, парсинг суммы, добавление позиции, список последних позиций.
- На главной (для авторизованных) — последние записи пользователя (до 20) и итог по ним.
- Базовая авторизация + редирект гостя на `auth/login`.

---
